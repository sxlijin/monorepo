#!/usr/bin/env bash

# Qt6 environment setup for qmetaobject-rs
export QT_DIR=$(brew --prefix qt)
export PATH="$QT_DIR/bin:$PATH"

# Development environment
export STEMS_LIVE_RELOAD=1

# Rust development
export RUST_LOG=debug